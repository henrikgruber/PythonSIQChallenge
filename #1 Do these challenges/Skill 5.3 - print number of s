# Modify the below program so it  print number of a's within the string and converts everything to uppercase and lowercase

s = "Strings are awesome!"
# Length should be 20
print("Length of s = %d" % len(s))

# Number of a's should be 2
#print("a occurs times:")

# Convert everything to uppercase
print("String in uppercase: %s")

# Convert everything to lowercase
print("String in lowercase: %s")




-----
Solution:



      s = "Strings are awesome!"
# Length should be 20
print("Length of s = %d" % len(s))

# Number of a's should be 2
print("a occurs times: " + str(s.count('a')))

# Convert everything to uppercase
print("String in uppercase: %s" % s.upper())

# Convert everything to lowercase
print("String in lowercase: %s" % s.lower())

    
    
      
  
            